This file notes that the Customizer Top Bar settings are deprecated and moved to the admin Header Options > Top Bar.

The customizer hooks were removed to avoid duplicate settings.

If you want to fully delete `customizer-topbar.php` later, ensure no other code depends on its helper functions.